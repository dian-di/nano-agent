# Nano Agent Knowledge

TypeScript research skeleton for studying Knowledge Retrieval, Tool Discovery, Tool Usage Knowledge, JIT Context, and Agent Runtime boundaries.

The Agent runtime is intentionally isolated behind `src/runtime/`; the core research surface lives in `knowledge/`, `tools/`, `context/`, `orchestration/`, and `evals/`.

All implementation methods are intentionally left as `Not implemented`.
